import React from 'react';
import { BottomSheetProps } from '../types/types';
declare const BottomSheet: ({ isVisible, children, height, onHide }: BottomSheetProps) => React.JSX.Element;
export default BottomSheet;
